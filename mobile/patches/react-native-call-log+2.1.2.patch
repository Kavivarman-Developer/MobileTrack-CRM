diff --git a/node_modules/react-native-call-log/android/build.gradle b/node_modules/react-native-call-log/android/build.gradle
index 302ff71..d3e6635 100644
--- a/node_modules/react-native-call-log/android/build.gradle
+++ b/node_modules/react-native-call-log/android/build.gradle
@@ -1,14 +1,3 @@
-buildscript {
-  repositories {
-    google()
-    jcenter()
-  }
-
-  dependencies {
-    classpath 'com.android.tools.build:gradle:3.3.0'
-  }
-}
-
 apply plugin: 'com.android.library'
 
 def safeExtGet(prop, fallback) {
@@ -16,6 +5,7 @@ def safeExtGet(prop, fallback) {
 }
 
 android {
+  namespace "com.wscodelabs.callLogs"
   compileSdkVersion safeExtGet('compileSdkVersion', 28)
 
   defaultConfig {
@@ -31,11 +21,11 @@ android {
 
 repositories {
     google()
+    mavenCentral()
     maven {
         // All of React Native (JS, Obj-C sources, Android binaries) is installed from npm
         url "$rootDir/../node_modules/react-native/android"
     }
-    jcenter()
 }
 
 dependencies {
